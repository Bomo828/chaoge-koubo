# 参与贡献

感谢你对爆点实验室的关注。提交代码前请先阅读 README、架构说明和安全策略。

## 开发流程

1. 从 `main` 创建功能分支。
2. 每个提交只处理一个清晰问题，不混入生成产物或个人配置。
3. 运行类型检查、代码检查和生产构建。
4. 涉及界面的改动附上前后截图；涉及视频模板的改动附上短样片和模板隔离检查结果。
5. Pull Request 说明修改目的、验证方法、风险和回滚方式。

```bash
pnpm install --frozen-lockfile
pnpm run typecheck
pnpm run lint
pnpm run build
```

## 代码与数据边界

- 不提交 `.env.local`、数据库、日志、缓存、用户上传或生成资产。
- 不提交 API Key、云密钥、支付密钥、证书、Token、Cookie 或 SSH 私钥。
- 不提交没有明确再分发许可的字体、音效、BGM、图片、视频和模板素材。
- 示例配置只能使用 `example.com`、示例内网地址和无效占位值。
- 新增第三方服务时，密钥必须留在服务端，并补充环境变量和部署文档。

## 视频模板

- 模板资源必须独立，不能在未声明的情况下继承其他模板的字体、音效或 BGM。
- 改动字幕、标题、音效、BGM 或转场时，应验证模板 9–12 均不受意外影响。
- 测试视频、参考原片和商业素材不进入 Git；只提交规则、代码和获准再分发的资源。

## 安全问题

不要用公开 Issue 报告可利用漏洞或泄露密钥。请按照 [SECURITY.md](SECURITY.md) 使用私下安全报告渠道。
