# 模板9「红白双语」｜固定原片排版对齐规范

固定原片：`微信视频2026-08-05_114729_279.mp4`

测量基准：1080×1920，主要使用原片 7 秒内容帧；位置为人工像素复核后的近似安全值，不复制原片固定文案。

## 镜头节奏

- 原片在语义节点使用明显的全景、轻近景和重点近景切换，而不是固定缩放。
- 模板按钩子、反差、观点、数字、步骤、品牌和 CTA 选择 1.00–1.17 的景别。
- 核心观点、数字、品牌和 CTA 可先用约 0.12 秒回到全景，再快速落入近景；禁止固定秒数循环。

| 项目 | 原片观察值 | 模板9 V14 | 规则 |
|---|---:|---:|---|
| 标题顶部 | 约 105–112 px | 约 109 px | 保持在顶部安全区内，不贴边 |
| 标题右边距 | 约 65–75 px | 约 66 px | 两排统一右对齐 |
| 小标题字号 | 约 76–80 px | 78 px | 白字、6.8 px 红色圆角描边 |
| 主标题字号 | 约 138–146 px | 142 px | 红字、9.5 px 白色圆角描边 |
| 标题字重 | Heavy / Black | 900 | 使用 Noto Serif SC Black |
| 标题字距 | 紧缩 | -4 / -8 px | 小标题和主标题分别控制 |
| 标题阴影 | 右下柔阴影 | 8 px / 4 px | 禁止硬偏移双影 |
| 中文字幕顶部 | 约 1185–1210 px | 1192 px | 固定在下三分区左侧 |
| 中文字幕字号 | 约 80–84 px | 82 px | Noto Serif SC，字重 880 |
| 关键词比例 | 约 1.24–1.30 倍 | 1.26 倍 | 红字白边，仅突出完整词组 |
| 中文字幕描边 | 约 5 px 黑边 | 5.2 px | 圆角、圆端、先描边后填充 |
| 关键词描边 | 约 6 px 白边 | 6 px | 禁止黑底色块 |
| 英文译文 | 约 31–35 px | 34 px | Arial Bold，紧贴中文下方 |
| 单屏中文 | 约 4–7 字 | 平均 5.42 字 | 必须按完整语义断句 |

## 声音规则

- 背景音乐使用完整曲目单次播放；模板9禁用 `loop`。
- 当前 AI/课程内容匹配 `corporate-soft`，长度约 140 秒，超过本次 32.85 秒成片，不会发生首尾重播。
- 背景音乐保持 0.115，口播期间持续压低。
- 音效只绑定钩子、反差、观点、数字、步骤和 CTA；V14 平均节点音量约 0.181。

## 每版必须复核

- 生成固定原片 Markdown + JSON 对比报告。
- 检查标题右对齐、字幕下三分区、英文译文和关键词比例。
- 检查完整曲目未循环、音效可听但不遮挡人声。
- 解码并检查全部成片帧，坏帧必须为 0。
