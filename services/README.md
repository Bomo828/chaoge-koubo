# Local media services

`services/` contains the separately deployed video and Remotion workers used by Merchant Studio. The web application continues to deploy from the repository root; worker updates are released independently to `api.chaogeai.top`.
