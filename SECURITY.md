# 安全策略

## 报告安全问题

请不要在公开 Issue 中提交可利用细节、真实账号、订单、密钥或用户数据。仓库所有者应在 GitHub 开启 Private Vulnerability Reporting，并通过该渠道接收漏洞报告。

## 凭证管理

- API Key、云密钥、支付密钥、证书和 SSH 私钥只保存在服务器受限目录、密钥管理服务或 GitHub Environment Secrets。
- 前端只能调用本站服务端 API，不得直接持有第三方服务密钥。
- 开发、测试、生产使用不同凭证；密钥按权限最小化并定期轮换。
- 日志和错误响应不得打印 Authorization、Cookie、签名、私钥、完整手机号或支付敏感字段。
- 任何凭证一旦进入 Git 历史、截图或聊天记录，应立即吊销并更换。

## 会员、积分与支付

- 密码使用强哈希，不保存明文；登录接口需要限流和防暴力破解。
- 管理后台强制单独权限校验，不能只依靠前端隐藏入口。
- 微信支付以服务端查询/验签后的订单结果为准，不信任浏览器成功页面。
- 支付回调必须验证签名、解密通知、校验金额与商户身份，并保证幂等入账。
- 积分余额变更需保留不可篡改的流水、业务来源、操作者和关联订单。

## 文件与视频任务

- 校验文件类型、大小和实际媒体格式，不信任扩展名。
- 用户文件使用随机对象键并做租户隔离；私有文件使用签名 URL。
- FFmpeg、浏览器和第三方解析器在受限用户下运行，并设置时间、内存和磁盘上限。
- 临时文件按生命周期自动清理；用户删除资产时同步处理对象存储和数据库记录。

## 开源发布

公开前按照 [开源发布指南](docs/OPEN_SOURCE_GUIDE.md) 执行完整 Git 历史扫描，并确认第三方素材再分发权。生产实施记录、真实基础设施标识、用户数据和商业素材不进入公开仓库。
